#include <stdio.h>
#include <time.h>

int main(){
    struct Account{ //Creates the account setup and login
        char username[20];
        char password[50];
        char FullName[50];
    };
    int loadingtime; //Makes loading seem like ., .., ...
    while(loadingtime < 5){
        printf("Loading.")
    }
    
}